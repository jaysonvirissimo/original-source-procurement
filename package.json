{
  "name": "osp",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit -p tsconfig.json && pnpm -r typecheck",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:browser": "pnpm --filter @osp/game test:browser",
    "build": "pnpm --filter @osp/game build",
    "corpus:import": "node tools/mgs-importer/src/bin/import.ts",
    "corpus:update": "node apps/game/build/bin/update-corpus.ts",
    "corpus:verify": "node apps/game/build/bin/verify-corpus.ts",
    "corpus:write": "node tools/mgs-importer/src/bin/write.ts",
    "corpus:spans": "node tools/mgs-importer/src/bin/spans.ts",
    "corpus:decode": "node tools/mgs-importer/src/bin/decode.ts",
    "curriculum:validate": "node packages/curriculum/src/bin/validate.ts",
    "curriculum:targets": "node apps/game/build/bin/generate-targets.ts",
    "audit:upstream": "node apps/game/build/bin/audit-upstream.ts apps/game/dist",
    "audit:distribution": "node apps/game/build/bin/audit-distribution.ts apps/game/dist",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm curriculum:validate && pnpm build && pnpm audit:distribution && pnpm audit:upstream"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "5.0.0",
    "eslint": "10.10.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.6",
    "globals": "17.12.0",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "8.70.0",
    "vitest": "5.0.0"
  }
}
